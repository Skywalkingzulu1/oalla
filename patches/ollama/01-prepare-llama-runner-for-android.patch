diff --git a/external/ollama/llm/server.go b/external/ollama/llm/server.go
index 7d921f1..3a7b1ff 100644
--- a/external/ollama/llm/server.go
+++ b/external/ollama/llm/server.go
@@ -35,6 +35,9 @@ import (
 	"github.com/ollama/ollama/model"
 )
 
+// Used to inject the runner logic, avoiding direct imports
+var RunRunnerFunc func(args []string) error
+
 type filteredEnv []string
 
 func (e filteredEnv) LogValue() slog.Value {
@@ -79,6 +82,7 @@ type LlamaServer interface {
 // llmServer is an instance of the llama.cpp server
 type llmServer struct {
 	port        int
+	inProcess   bool // If true, the server is running in-process
 	cmd         *exec.Cmd
 	done        chan error // Channel to signal when the process exits
 	status      *StatusWriter
@@ -332,7 +336,7 @@ func NewLlamaServer(gpus discover.GpuInfoList, modelPath string, f *ggml.GGML, a
 		}
 		if port == 0 {
 			slog.Debug("ResolveTCPAddr failed, using random port")
-			port = rand.Intn(65535-49152) + 49152 // get a random port in the ephemeral range
+			port = rand.Intn(9501-9000) + 9000 // get a random port in the ephemeral range from 9000-9500
 		}
 		finalParams := []string{"runner"}
 		if textProcessor != nil {
@@ -378,10 +382,17 @@ func NewLlamaServer(gpus discover.GpuInfoList, modelPath string, f *ggml.GGML, a
 
 		// finally, add the root library path
 		libraryPaths = append(libraryPaths, discover.LibOllamaPath)
-
+		// check from RunRunnerFunc if we should run in-process or subprocess
+		usedRunner := "ollama runner command line"
+		useInProcessRunner := false
+		if RunRunnerFunc != nil {
+			usedRunner = "function based runner"
+			useInProcessRunner = true
+		}
 		s := &llmServer{
 			port:          port,
-			cmd:           exec.Command(exe, finalParams...),
+			inProcess:     useInProcessRunner,
+			cmd:           nil, // only set if subprocess is used
 			status:        NewStatusWriter(os.Stderr),
 			options:       opts,
 			modelPath:     modelPath,
@@ -395,6 +406,29 @@ func NewLlamaServer(gpus discover.GpuInfoList, modelPath string, f *ggml.GGML, a
 			done:          make(chan error, 1),
 		}
 
+		if !useInProcessRunner {
+			s.cmd = exec.Command(exe, finalParams...)
+		}
+
+		// print debug logs
+		slog.Debug(
+			"params for runner of llama.cpp",
+			"params", finalParams,
+			"runner_used", usedRunner,
+		)
+
+		if useInProcessRunner {
+			go func() {
+				if RunRunnerFunc == nil {
+					s.done <- fmt.Errorf("RunRunnerFunc is not set")
+					return
+				}
+				err := RunRunnerFunc(finalParams[1:])
+				s.done <- err
+			}()
+			return s, nil
+		}
+
 		s.cmd.Env = os.Environ()
 		s.cmd.Stdout = os.Stdout
 		s.cmd.Stderr = s.status
@@ -506,19 +540,19 @@ type ServerStatusResponse struct {
 }
 
 func (s *llmServer) getServerStatus(ctx context.Context) (ServerStatus, error) {
-	// Fail fast if its exited
-	if s.cmd.ProcessState != nil {
+	// For subprocess runners, fail fast if it has exited
+	if !s.inProcess && s.cmd != nil && s.cmd.ProcessState != nil {
 		msg := ""
 		if s.status != nil && s.status.LastErrMsg != "" {
 			msg = s.status.LastErrMsg
 		}
 		if s.cmd.ProcessState.ExitCode() == -1 {
-			// Most likely a signal killed it, log some more details to try to help troubleshoot
 			slog.Warn("llama runner process no longer running", "sys", s.cmd.ProcessState.Sys(), "string", s.cmd.ProcessState)
 		}
 		return ServerStatusError, fmt.Errorf("llama runner process no longer running: %d %s", s.cmd.ProcessState.ExitCode(), msg)
 	}
 
+	// Perform health check over HTTP
 	req, err := http.NewRequestWithContext(ctx, http.MethodGet, fmt.Sprintf("http://127.0.0.1:%d/health", s.port), nil)
 	if err != nil {
 		return ServerStatusError, fmt.Errorf("error creating GET request: %v", err)
@@ -592,69 +626,50 @@ func (s *llmServer) Ping(ctx context.Context) error {
 
 func (s *llmServer) WaitUntilRunning(ctx context.Context) error {
 	start := time.Now()
-	stallDuration := envconfig.LoadTimeout()    // If no progress happens
-	stallTimer := time.Now().Add(stallDuration) // give up if we stall
+	stallDuration := 10 * time.Second // Replace with envconfig.LoadTimeout() if needed
+	stallTimer := time.Now().Add(stallDuration)
 
 	slog.Info("waiting for llama runner to start responding")
 	var lastStatus ServerStatus = -1
-	fullyLoaded := false
+	// fullyLoaded := false
 
 	for {
 		select {
 		case <-ctx.Done():
-			slog.Warn("client connection closed before server finished loading, aborting load")
 			return fmt.Errorf("timed out waiting for llama runner to start: %w", ctx.Err())
 		case err := <-s.done:
-			return fmt.Errorf("llama runner process has terminated: %w", err)
+			if err != nil {
+				return fmt.Errorf("runner terminated early: %w", err)
+			}
 		default:
 		}
+
 		if time.Now().After(stallTimer) {
-			// timeout
-			msg := ""
-			if s.status != nil && s.status.LastErrMsg != "" {
-				msg = s.status.LastErrMsg
-			}
-			return fmt.Errorf("timed out waiting for llama runner to start - progress %0.2f - %s", s.loadProgress, msg)
+			return fmt.Errorf("timed out waiting for llama runner to start - progress %0.2f", s.loadProgress)
 		}
-		if s.cmd.ProcessState != nil {
-			msg := ""
-			if s.status != nil && s.status.LastErrMsg != "" {
-				msg = s.status.LastErrMsg
-			}
-			return fmt.Errorf("llama runner process no longer running: %d %s", s.cmd.ProcessState.ExitCode(), msg)
-		}
-		ctx, cancel := context.WithTimeout(ctx, 200*time.Millisecond)
-		defer cancel()
-		priorProgress := s.loadProgress
-		status, _ := s.getServerStatus(ctx)
+
+		ctx2, cancel := context.WithTimeout(ctx, 200*time.Millisecond)
+		status, _ := s.getServerStatus(ctx2)
+		cancel()
+
 		if lastStatus != status && status != ServerStatusReady {
-			// Only log on status changes
-			slog.Info("waiting for server to become available", "status", status)
+			slog.Info("waiting for server", "status", status)
 		}
+
 		switch status {
 		case ServerStatusReady:
 			s.loadDuration = time.Since(start)
-			slog.Info(fmt.Sprintf("llama runner started in %0.2f seconds", s.loadDuration.Seconds()))
+			slog.Info(fmt.Sprintf("llama runner ready in %0.2fs", s.loadDuration.Seconds()))
 			return nil
 		default:
 			lastStatus = status
-			// Reset the timer as long as we're making forward progress on the load
-			if priorProgress != s.loadProgress {
-				slog.Debug(fmt.Sprintf("model load progress %0.2f", s.loadProgress))
-				stallTimer = time.Now().Add(stallDuration)
-			} else if !fullyLoaded && int(s.loadProgress*100.0) >= 100 {
-				slog.Debug("model load completed, waiting for server to become available", "status", status)
-				stallTimer = time.Now().Add(stallDuration)
-				fullyLoaded = true
-			}
-			time.Sleep(time.Millisecond * 250)
-			continue
+			time.Sleep(250 * time.Millisecond)
 		}
 	}
 }
 
 func (s *llmServer) Pid() int {
-	if s.cmd != nil && s.cmd.Process != nil {
+	if !s.inProcess && s.cmd != nil && s.cmd.Process != nil {
 		return s.cmd.Process.Pid
 	}
 	return -1
@@ -739,8 +754,6 @@ func (s *llmServer) Completion(ctx context.Context, req CompletionRequest, fn fu
 	if len(req.Format) > 0 {
 		switch string(req.Format) {
 		case `null`, `""`:
-			// Field was set, but "missing" a value. We accept
-			// these as "not set".
 			break
 		case `"json"`:
 			req.Grammar = grammarJSON
@@ -748,8 +761,6 @@ func (s *llmServer) Completion(ctx context.Context, req CompletionRequest, fn fu
 			if req.Format[0] != '{' {
 				return fmt.Errorf("invalid format: %q; expected \"json\" or a valid JSON Schema object", req.Format)
 			}
-
-			// User provided a JSON schema
 			g := llama.SchemaToGrammar(req.Format)
 			if g == nil {
 				return fmt.Errorf("invalid JSON schema in format")
@@ -773,12 +784,10 @@ func (s *llmServer) Completion(ctx context.Context, req CompletionRequest, fn fu
 	}
 	defer s.sem.Release(1)
 
-	// put an upper limit on num_predict to avoid the model running on forever
 	if req.Options.NumPredict < 0 || req.Options.NumPredict > 10*s.options.NumCtx {
 		req.Options.NumPredict = 10 * s.options.NumCtx
 	}
 
-	// Make sure the server is ready
 	status, err := s.getServerStatusRetry(ctx)
 	if err != nil {
 		return err
@@ -786,7 +795,6 @@ func (s *llmServer) Completion(ctx context.Context, req CompletionRequest, fn fu
 		return fmt.Errorf("unexpected server status: %s", status)
 	}
 
-	// Handling JSON marshaling with special characters unescaped.
 	buffer := &bytes.Buffer{}
 	enc := json.NewEncoder(buffer)
 	enc.SetEscapeHTML(false)
@@ -822,14 +830,15 @@ func (s *llmServer) Completion(ctx context.Context, req CompletionRequest, fn fu
 	buf := make([]byte, 0, maxBufferSize)
 	scanner.Buffer(buf, maxBufferSize)
 
-	// keep track of the last token generated, this is used to abort if the model starts looping
 	var lastToken string
 	var tokenRepeat int
+	var fullContent strings.Builder
+	fmt.Println("Content Logs:")
+	fmt.Print("  ") // indent the content line once
 
 	for scanner.Scan() {
 		select {
 		case <-ctx.Done():
-			// This handles the request cancellation
 			return ctx.Err()
 		default:
 			line := scanner.Bytes()
@@ -846,6 +855,7 @@ func (s *llmServer) Completion(ctx context.Context, req CompletionRequest, fn fu
 			if err := json.Unmarshal(evt, &c); err != nil {
 				return fmt.Errorf("error unmarshalling llm prediction response: %v", err)
 			}
+
 			switch {
 			case strings.TrimSpace(c.Content) == lastToken:
 				tokenRepeat++
@@ -854,13 +864,15 @@ func (s *llmServer) Completion(ctx context.Context, req CompletionRequest, fn fu
 				tokenRepeat = 0
 			}
 
-			// 30 picked as an arbitrary max token repeat limit, modify as needed
 			if tokenRepeat > 30 {
 				slog.Debug("prediction aborted, token repeat limit reached")
 				return ctx.Err()
 			}
 
 			if c.Content != "" {
+				fmt.Print(c.Content) // Print the content directly to stdout
+				fullContent.WriteString(c.Content)
+
 				fn(CompletionResponse{
 					Content: c.Content,
 				})
@@ -868,6 +880,7 @@ func (s *llmServer) Completion(ctx context.Context, req CompletionRequest, fn fu
 
 			if c.Done {
 				fn(c)
+				fmt.Println() // Final newline
 				return nil
 			}
 		}
@@ -884,7 +897,6 @@ func (s *llmServer) Completion(ctx context.Context, req CompletionRequest, fn fu
 			}
 			return fmt.Errorf("an error was encountered while running the model: %s", msg)
 		}
-
 		return fmt.Errorf("error reading llm response: %v", err)
 	}
 
@@ -1027,20 +1039,20 @@ func (s *llmServer) Close() error {
 	}
 	s.llamaModelLock.Unlock()
 
-	if s.cmd != nil {
+	if !s.inProcess && s.cmd != nil {
 		slog.Debug("stopping llama server", "pid", s.Pid())
 		if err := s.cmd.Process.Kill(); err != nil {
 			return err
 		}
-		// if ProcessState is already populated, Wait already completed, no need to wait again
+		// Wait for the process to exit if not already
 		if s.cmd.ProcessState == nil {
 			slog.Debug("waiting for llama server to exit", "pid", s.Pid())
 			<-s.done
 		}
-
 		slog.Debug("llama server stopped", "pid", s.Pid())
 	}
 
+	// In-process runner: close signal is already propagated via s.done
 	return nil
 }
 
